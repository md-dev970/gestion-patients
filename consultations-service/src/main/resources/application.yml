# ═══════════════════════════════════════════════════════════════════════════════
# CONSULTATIONS SERVICE CONFIGURATION
# Conforme au Kit Commun - Microservice Consultations
# ═══════════════════════════════════════════════════════════════════════════════

server:
  port: 8086

spring:
  application:
    name: consultations-service

  datasource:
    url: jdbc:postgresql://localhost:5437/hospital_consultations
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    root: INFO
    com.hospital.consultation: DEBUG

# Swagger/OpenAPI Configuration
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

---
# Profil Docker
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:postgresql://postgres-consultations:5432/hospital_consultations

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/
